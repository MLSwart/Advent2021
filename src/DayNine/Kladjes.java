package DayNine;

/*
import java.util.HashMap;

public class Kladjes {
    public void checkBasinSize(int[][] lowPointCoordArray, int[][] heightMap, int lowPointArrayRegel) {
        HashMap<Integer, Integer> checked= new HashMap<Integer, Integer>();
        int xCoord = smoke1.lowPointCoordArray[lowPointArrayRegel][0];
        int yCoord = smoke1.lowPointCoordArray[lowPointArrayRegel][1];
        checked.put(xCoord, yCoord);
        boolean negenGevonden=false;
        boolean inChecked=checked.containsValue(xCoord)&&checked.containsKey(yCoord);
        while(!negenGevonden){
            xCoord--;
            if (heightMap[xCoord][yCoord]!=9&&xCoord>=0&&inChecked==false){ //links kijken
                checked.put(xCoord,yCoord);
            }
            else negenGevonden=true;

        }
        System.out.print(checked);

    }
}

Iterator<Point> i = smoke.lowPoints.iterator();
        while (i.hasNext())
            System.out.println(i.next());

if ((i.next().x() - 1) > 1 && heightMap[i.next().x() - 1][i.next().y()] != 9) { //kijk links
                seenPoints.add(new Point(i.next().x() - 1, i.next().y()));

            }

 */
